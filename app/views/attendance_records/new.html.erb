<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h3 class="card-title mb-0">
                        <i class="fas fa-clock me-2"></i>Registro de Marcación
                    </h3>
                </div>
                <div class="card-body">
                    <%= form_with model: @attendance, data: { turbo: false } do |f| %>
                        <div class="mb-3">
                            <%= f.label :record_type, "Tipo de registro : ", class: "form-label fw-bold" %>
                            <%= f.select :record_type,
                                        AttendanceRecord.record_types_for_select,
                                        { prompt: "Selecciona una opción" },
                                        { class: "form-select", required: true } %>
                        </div>
                        <div class="mb-3">
                            <%= f.label :comment, "Observaciones : ", class: "form-label fw-bold" %>
                            <%= f.text_area :comment,
                                class: "form-control",
                                rows: 4,
                                placeholder: "Escriba sus observaciones de forma puntual de ser necesario." %>
                        </div>
                        <div class="d-grid">
                            <%= f.submit "Registrar Asistencia",
                                class: "btn btn-primary btn-lg" %>
                        </div>
                    <% end %>
                </div>
                <div class="card-footer text-muted text-center">
                    <small>Fecha y hora se registrarán automáticamente</small>
                </div>
            </div>
        </div>
    </div>
</div>